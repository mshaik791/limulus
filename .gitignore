# Signing key and the record chain: never commit these.
# data/held-out/ is the private half of the Lab: this repo is public, and a
# held-out evaluation set anyone can read certifies nothing. The threat model
# lives in src/bench/families.ts on purpose; only the instances are secret.
data/

# Derived: the warehouse projection is rebuilt from the chain, never committed.
build/

# Node
node_modules/
npm-debug.log*

# Editors and OS
.DS_Store
.vscode/
.idea/

# Env
.env
.env.*
# ...except the template, which holds names and no values.
!.env.example
.vercel
.env*

# The build/red-team prompt is internal and sensitive: never commit it.
LIMULUS_CLAUDE_PROMPT.md
docs/design/*.pdf
